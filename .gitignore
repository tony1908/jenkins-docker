# Generated Kubernetes credentials for Dockerized Jenkins.
kubeconfig/*
!kubeconfig/.gitkeep

# Local VCS metadata embedded in sample fixtures.
sample-app/.git/
svn-working-copy/.svn/

# Local runtime/config files.
.env
.env.*
!.env.example
*.tmp
*.log

# Local Maven/JFrog credential files if someone creates them inside this repo.
.m2/
settings.xml
settings-security.xml

# Docker/Jenkins runtime data if bind-mounted during experiments.
jenkins_home/
docker_data/
registry_data/
