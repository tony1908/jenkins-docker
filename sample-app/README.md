# Jenkins DinD Sample App

This tiny Node app is used to verify that Jenkins can:

- clone local code,
- run tests inside a Docker container,
- build a Docker image,
- run the built image through Docker-in-Docker.
