apiVersion: v1
kind: Service
metadata:
  name: jenkins-dind-sample
  labels:
    app: jenkins-dind-sample
spec:
  selector:
    app: jenkins-dind-sample
  ports:
    - name: http
      port: 80
      targetPort: 3000
