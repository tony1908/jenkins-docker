apiVersion: v1
kind: Service
metadata:
  name: svn-dind-sample
  labels:
    app: svn-dind-sample
spec:
  selector:
    app: svn-dind-sample
  ports:
    - name: http
      port: 80
      targetPort: 3000
